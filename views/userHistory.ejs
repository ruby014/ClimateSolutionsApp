<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login History</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>
<body>
    <%- include('partials/navbar', {page:''})%>
    <div class="container mx-auto p-4">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center">
                <div class="max-w-md">
                    <h1 class="text-5xl font-bold"><%= userName %></h1>
                    <p class="py-6">User History: <%= email %></p>
                </div>
            </div>
        </div>
    <br> 

    <!-- table
    dont really know if this is done right - probably not  
    -->
    <table class="table table-zebra">
        <tbody>
                <tr>
                    <th>Login Date/Time</th>
                    <td><%= session.user.loginHistory.dateTime %></td>
                </tr>
                <tr>
                    <th>Client Information</th>
                    <td><%= session.user.loginHistory.userAgent %></td>
                </tr>
        </tbody>
    </table>
    </div>
</body>
</html>